[package]
name = "{{project-name}}"
version = "0.1.0"
authors = ["{{authors}}"]
edition = "2021"

[dependencies]
libc = { version = "0.2", default-features = false }
pros = { git = "https://github.com/serxka/pros-rs" }

